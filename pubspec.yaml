name: tbib_smooth_page_indicator
description: A smooth page indicator Package.
version: 0.0.1
homepage: https://github.com/the-best-is-best/tbib_smooth_page_indicator

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  

dev_dependencies:
  flutter_test:
    sdk: flutter